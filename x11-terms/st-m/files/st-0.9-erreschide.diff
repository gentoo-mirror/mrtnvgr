diff --git a/st.c b/st.c
index 62def59..880986e 100644
--- a/st.c
+++ b/st.c
@@ -1602,9 +1602,11 @@ csihandle(void)
 	switch (csiescseq.mode[0]) {
 	default:
 	unknown:
-		fprintf(stderr, "erresc: unknown csi ");
-		csidump();
-		/* die(""); */
+		if (erreschide == 0) {
+			fprintf(stderr, "erresc: unknown csi ");
+			csidump();
+			/* die(""); */
+		}
 		break;
 	case '@': /* ICH -- Insert <n> blank char */
 		DEFAULT(csiescseq.arg[0], 1);
@@ -1955,8 +1957,10 @@ strhandle(void)
 		return;
 	}
 
-	fprintf(stderr, "erresc: unknown str ");
-	strdump();
+	if (erreschide == 0) {
+		fprintf(stderr, "erresc: unknown str ");
+		strdump();
+	}
 }
 
 void
diff --git a/st.h b/st.h
index fd3b0d8..19a1b76 100644
--- a/st.h
+++ b/st.h
@@ -124,3 +124,4 @@ extern unsigned int tabspaces;
 extern unsigned int defaultfg;
 extern unsigned int defaultbg;
 extern unsigned int defaultcs;
+extern double erreschide;
diff --git a/x.c b/x.c
index aa09997..7479ca1 100644
--- a/x.c
+++ b/x.c
@@ -242,6 +242,7 @@ static int frccap = 0;
 static char *usedfont = NULL;
 static double usedfontsize = 0;
 static double defaultfontsize = 0;
+double erreschide = 1;
 
 static char *opt_class = NULL;
 static char **opt_cmd  = NULL;
@@ -2017,11 +2018,11 @@ run(void)
 void
 usage(void)
 {
-	die("usage: %s [-aiv] [-c class] [-f font] [-g geometry]"
+	die("usage: %s [-aivd] [-c class] [-f font] [-g geometry]"
 	    " [-n name] [-o file]\n"
 	    "          [-T title] [-t title] [-w windowid]"
 	    " [[-e] command [args ...]]\n"
-	    "       %s [-aiv] [-c class] [-f font] [-g geometry]"
+	    "       %s [-aivd] [-c class] [-f font] [-g geometry]"
 	    " [-n name] [-o file]\n"
 	    "          [-T title] [-t title] [-w windowid] -l line"
 	    " [stty_args ...]\n", argv0, argv0);
@@ -2055,6 +2056,9 @@ main(int argc, char *argv[])
 	case 'i':
 		xw.isfixed = 1;
 		break;
+	case 'd':
+		erreschide = 0;
+		break;
 	case 'o':
 		opt_io = EARGF(usage());
 		break;
